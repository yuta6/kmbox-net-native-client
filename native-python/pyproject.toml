[project]
name = "kmbox-net-python"
version = "0.1.0"
description = "Prototype of a library to control Kmbox Net. Implemented using Python's socket module"
requires-python = ">=3.10"
dependencies = []
